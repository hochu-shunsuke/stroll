/**
 * 透明なものを描く順番。数字が大きいほど後に描かれ、上に乗る。
 *
 * 本来 three は「カメラから遠いものを先に」並べ替えてくれるが、
 * 水面はカメラに追従する 1 枚の板なので常に「一番近い」と判定されてしまう。
 * その結果ほかの透明なものが軒並み水の下敷きになるので、順番を明示している。
 *
 * ここは 3 つのファイルにまたがる約束なので、値を散らばらせないこと。
 */
export const RENDER_ORDER = {
  /** 空。何より先に描き、あとから全部で上書きする。 */
  sky: -1000,
  /** 海と湖。地形より後。 */
  water: 10,
  /** 名前の板。水の上に出さないと、水辺で読めなくなる。 */
  label: 20,
} as const;
