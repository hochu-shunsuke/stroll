# 作業メモ

シードから生成される自然を歩く世界。全体像と拡張の判断基準は `README.md` にある。

`.claude/` があれば、**コードを触る前に読むこと**。方向性の判断と、
一度やって取り消したことの記録が入っている（git には入れていない）。
同じ提案を繰り返さないために置いてある。

## 増やすかどうかの判断

> **`(x, z)` とシードだけから、純粋な関数として計算できるか？**

これを満たす限り、無限生成も URL 共有も壊れない。満たさないもの（川の流れなど、
大域の情報が要るもの）は、入れる前に設計ごと考え直すこと。

`heightAt()` は 1 チャンクあたり約 9,400 回呼ばれる。層を足すほど生成が重くなる。

## 世界の作られ方（地形・気候・宝物）

3 つの層が重なって景色ができる。すべて `(x,z)` とシードの純粋関数。

1. **標高** `terrain.heightAt()` — **ノイズを足すのではなく、3 つのパラメータを
   スプラインに通す**（大陸度・侵食度・ねじれ）。詳しくは下の節。
   洞窟は作らない（高さマップなので原理的に出ない）。
2. **気候** 気温 × 湿り気で気候帯（砂漠・草原・森・密林・寒帯・雪）。
   **どちらも地形から作る。** 気温は標高で下がり、湿り気は**雨陰**（風上に自分より
   高い山があると乾く）で決まる。色は `terrain.shade()`、植生は `world/scatter.ts`。
3. **宝物** `world/special.ts` — まれに現れ、気候を無視して上書きするレア区画。
   秋・桜・青草原など。追加は `SPECIAL_BIOMES` に `forest()`/`meadow()` で 1 行。
   全く別の宝物（霧など）は生の `SpecialBiome` を書く。型は平たいまま残してある。
   植生の種類番号は `world/vegetationKinds.ts`（循環を避ける中立点）。

宝物を足す手順: ①`SPECIAL_BIOMES` に 1 行 ②木が要るなら `vegetationKinds` に
KIND を足し `render/treeShape.ts` の `TREE_CATALOG` に形を足す。3 ファイルを番号で結ぶ。

## 標高の作り方（ここが一番大きい）

**ノイズを足し合わせない。3 つのパラメータをスプラインに通す。**

```
大陸度 ──→ SP_BASE ────────→ 基準の高さ（外洋 -58m 〜 奥地 142m）
侵食度 ──→ SP_RELIEF ───┐
大陸度 ──→ SP_RELIEF_GATE ┴→ 起伏の振れ幅
ねじれ ──→ 折り返し → SP_PV → 尾根と谷（谷底は平ら＝歩く回廊）
```

**折れ点が地形の種類を作る。** 出力が同じ値で並ぶ区間が平原と台地に、
入力が詰まっている区間が崖と海岸線になる。掛け算では段を作れないので、
`ridged^a × flat^b × land^c` の形に戻さないこと（前はそれで、
「海の中に平らな島」以外を作れなかった）。

つまみ方の勘所:

- 海の割合は `SP_BASE` がゼロを跨ぐ入力値で決まる
- **低地の起伏は抑える。** `SP_RELIEF_GATE` が「歩く場所（低地）」と
  「見る場所（内陸）」を分けている。一様に上げると散歩にならない
- **海岸では起伏を殺す**（`COAST_FLAT_H`）。これが無いと起伏だけで海面線を
  何度も跨いでリアス式海岸になる

`heightAt()` の中では、**種類ごとの高さの式を 1 つしか評価しない**（`landform.ts`）。
全部評価すると生成が種類の数だけ重くなる。

## 静かに壊れる場所

型検査を通るのに壊れる。触るときはここを思い出すこと。

- **斜面を作るときは `smoothstep` の最大傾斜が平均の 1.5 倍であることを勘定に入れる。**
  「高さ H を幅 W で落とす」の実際の最大傾斜は `1.5 × H/W`。これを忘れて台地の縁と
  川岸で 2 回とも崖を作った（台地は見積もり 1.05 に対し実測 4.84）。
  `controller.ts` に `MAX_CLIMB` との突き合わせを起動時の検査として置いてある
- **地形の分布を変えたら、それに依存する下流を全部較正し直す。**
  高さの分布を変えたのに下流を放置して 4 か所同時に壊した（曲率の明暗が黒い斑に、
  侵食の減衰が等高線状の縞に、domain warp が指紋模様に、植生の閾値が旧分布のまま）。
  **測るのは `heightAt` ではなく、実際に描画される面の色**
- **重い関数を四角形ごとに呼ばない。** 雨陰で `moistureAt` が 40 倍になり、植生が
  1 チャンクで 3,311 回呼んで起動が 2 倍になった。気候は波長 1,100m 以上で
  ゆっくり変わるので、`CLIMATE_STEP`（24m）の格子で引いて補間する。
  この定数は `chunk.ts` と `scatter.ts` の両方にある
- **水面は海抜固定の 1 枚板。** だから**内陸の湖と、水のある川は原理的に作れない**。
  地面を海面下まで彫れば水は入るが、深く彫るほど低地が水没して海岸がリアス式になる
  （実測で水際の交差が 2.5 → 3.5 回/km）。谷筋は 9m の涸れ谷として入れてある。
  **水面の設計を変えずに `RIVER_DEPTH` を上げないこと**
- **`terrain.heightOnGrid()` と `chunk.buildChunkArrays()` は同じ三角形の切り方をしている前提。**
  判定は `terrain.splitsAlongMainDiagonal()` に 1 つだけ置いて両方から使う。
  ベタ書きで揃えていた頃は機構で守られていなかった
- **`LOD_STEPS` と `LOD_RINGS` は同じ長さ。** 起動時に検査している
- **Worker の生成は文字列リテラル**（`new URL('../world/worker.ts', import.meta.url)`）。
  ファイルを移動しても型検査は気づかず、ビルドで初めて落ちる
- **`EYE_HEIGHT` は通信で送る `y` の意味そのもの。** アバターの身長もこれに従う。
  値を変えると他人のアバターだけ地面に沈む
- **透明なものの描画順は `render/order.ts` に集約。** 水面はカメラ追従の板なので
  three の並べ替えでは常に「一番近い」。新しい透明物を足すと水の下敷きになる
- **本番の中継先は同一オリジンから決めており、環境変数を見ない。**
  `.env.local` はモードに関係なく本番ビルドでも読まれるため（一度これで壊した）
- **`world/special.ts` の宝物抽選はシードを混ぜる（`seedSalt`）。** 忘れると
  全世界で宝物の位置が同じになる（一度これで壊した）
- **非インデックスのジオメトリを頂点番号で変形しない。** 同じ角が複数頂点として
  重複しているので、番号で乱数を引くと面が裂ける。座標から引くこと（岩で踏んだ）
- **再接続の死体（幽霊アバター）対策は 2 段構え。** クライアントは cid
  （`sessionStorage` の固定 ID）を再接続・再読み込みでも同じにして送る。
  サーバは同じ cid の古い接続を閉じ、welcome からも除外する。
  cid をメモリだけに持つとスマホの再読み込みで変わり、死体が残る（踏んだ）。
  **未対処の穴**: 誰も再接続してこない死体（タブを閉じたまま close も届かない）は
  消す人がいない。実際に観測したら、サーバに「最後に見た時刻」を持たせて
  古い接続を掃除する（alarm か、通信時に間引く）。今は先回りしない。

## 合言葉と URL

- 合言葉は英数字 **8 文字固定**。決まりは `shared/seed.ts` にだけ書き、
  クライアントとサーバの両方が使う。**片方にだけ検証を足さないこと。**
- URL は `https://.../#k7p2mq9x`。合言葉は **`#`（ハッシュ）**に載る。
  `#` 以降はサーバに送られないので、打ち間違えても 404 が起きず、
  サーバの経路分けも要らない。クエリやパスに戻すとこの利点が消える。
- WebSocket だけは `/ws?seed=...` で内部的に合言葉を渡す（利用者は見ない）。

## 足さないもの

- **洞窟・穴・閉塞的なもの。** 高さマップなので原理的に発生しない。そのまま保つ
- **外部アセット。** 地形も植生も音も、その場で計算して作る。
  シードだけで世界が再現できることが URL 共有の前提になっている
- **目的・期限・競争。** 「目的も期限もない」ことが売り。義務を作る要素は価値を削る

## コマンド

```bash
npm run dev      # クライアント（vite。LAN 公開されるのでスマホからも見られる）
npm run relay    # 中継サーバ（別ターミナル。wrangler dev）
npm run ci       # 型(client/server)＋ビルド＋seed テスト。push 前にこれを通す
npm test         # seed の単体テストだけ
npm run deploy   # ビルドして Cloudflare へ。本体と中継が同時に出る
```

## CI / CD

デプロイは **GitHub Actions が唯一の経路**。手元から `npm run deploy` も打てるが、
通常は main に入れれば自動で出る。

```
PR           → ci.yml        : npm run ci（型・ビルド・テスト）だけ。出さない
main へ push → deploy.yml    : verify → 通ったときだけ deploy
                                └ 落ちたらここで止まる ＝ 壊れたものは本番に出ない
手動          → Actions 画面から deploy.yml を workflow_dispatch で実行
```

**Cloudflare 側の Git 連携は使わない（切ってある）。** 理由が 2 つある。

1. あの連携には**検証のゲートが無い**。CI が落ちていても main に入った瞬間に出る
2. デプロイの仕組みが**ダッシュボードの中にしか無く**、リポジトリを見ても分からない

もし将来ダッシュボードで再接続すると、Actions と二重にデプロイが走り、
古いビルドが新しいビルドを後から上書きしうる。**再接続しないこと。**

必要な Secrets（GitHub のリポジトリ設定に登録済み）:
`CLOUDFLARE_API_TOKEN`（Edit Cloudflare Workers テンプレートで発行）、
`CLOUDFLARE_ACCOUNT_ID`。

`concurrency: deploy` で同時実行を止めている。連続 push でも順番に出る。

中継サーバ相手のプロトコル確認はネットワーク依存なので CI に入れていない。
手元で `npm run relay` を立てて別途叩く。

## branch を切って進めた方がよい変更

見た目や地形を大きく触るときは branch を切り、まず `npm run dev` の
LAN URL でスマホ確認 → PR にすれば ci.yml が検証する → main へ merge すれば
自動でデプロイされる。プレビューを公開 URL で見たいときは Cloudflare の
Preview URLs を一度ダッシュボードで有効化する（未設定なので今は出せない）。
